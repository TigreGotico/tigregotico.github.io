---
import Base from '../layouts/Base.astro';
import { getEntry, render } from 'astro:content';
import { site } from '../data/site';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const page = await getEntry('pages', 'services');
const { Content } = await render(page!);
---

<Base title={page!.data.title} description={page!.data.description}>
  <header class="mb-10 max-w-2xl">
    <p class="eyebrow mb-2">Engagements</p>
    <h1 class="font-serif text-4xl font-bold">Services</h1>
    <p class="mt-3 text-muted">{page!.data.description}</p>
  </header>

  <article class="prose-content max-w-3xl">
    <Content />
  </article>

  <div class="mt-12 flex flex-wrap gap-3 rounded-xl border border-border bg-surface p-6">
    <p class="w-full font-serif text-lg font-semibold">Ready to scope an engagement?</p>
    <a class="btn-primary" href={`${base}/contact`}>Contact us</a>
    <a class="btn-ghost" href={`mailto:${site.email}`}>{site.email}</a>
  </div>
</Base>
