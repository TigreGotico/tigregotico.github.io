---
import Base from '../../layouts/Base.astro';
import PageHeader from '../PageHeader.astro';
import { getEntry, render } from 'astro:content';
import { pageEntryId, type Locale } from '../../i18n';

interface Props { locale?: Locale }
const locale: Locale = Astro.props.locale ?? 'en';
const S = {
  en: { eyebrow: 'No small print' },
  pt: { eyebrow: 'Sem letras pequenas' },
  es: {
    "eyebrow": "Sin letra pequeña"
  },
  de: {
    "eyebrow": "Kein Kleingedrucktes"
  },
  nl: {
    "eyebrow": "Geen kleine lettertjes"
  },
  fr: {
    "eyebrow": "Pas de petits caractères"
  },
  it: {
    "eyebrow": "Niente clausole nascoste"
  },
  ru: {
    "eyebrow": "Без мелкого шрифта"
  },
  ar: {
    "eyebrow": "بلا شروط خفية"
  },
  fa: {
    "eyebrow": "بدون شرط‌های ریز"
  },
  hi: {
    "eyebrow": "कोई छिपी शर्तें नहीं"
  },
  zh: {
    "eyebrow": "没有小字条款"
  },
  ko: {
    "eyebrow": "깨알 같은 약관 없음"
  },
  ja: {
    "eyebrow": "細かい注釈なし"
  }
}[locale];

const page = await getEntry('pages', pageEntryId(locale, 'privacy'));
const { Content } = await render(page!);
---

<Base title={page!.data.title} description={page!.data.description} locale={locale}>
  <PageHeader
    eyebrow={S.eyebrow}
    title={page!.data.title}
    lead={page!.data.description}
  />

  <article class="prose-content mx-auto max-w-3xl">
    <Content />
  </article>
</Base>
